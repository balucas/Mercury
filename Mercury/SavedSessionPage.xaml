<Page
    x:Class="Mercury.SavedSessionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mercury"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:qc="using:Ailon.QuickCharts"
    xmlns:api="using:API"
	Loaded="Page_Loaded"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView 
            x:Name="SessionList" 
            ItemsSource="{x:Bind SavedSessions}"
            SelectionMode="Multiple">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="api:SavedSession">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Bind TimeStamp}" Margin="20,20,20,20"/>
                        <TextBlock Text="{x:Bind SessionData.Count}" Margin="20,20,20,20"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button 
            Content="View Statistics"
            Click="View_Stats"
            HorizontalAlignment="Center"/>
    </Grid>
</Page>
